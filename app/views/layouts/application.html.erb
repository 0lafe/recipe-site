<!DOCTYPE html>
<html>
<head>
  <title>Recipedia</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= csrf_meta_tags %>
</head>
<body>

  <div class="Navbar">
    <div class="top-bar-left">
      <span class="nav-links"> <%= link_to  'Recipes', "/recipes" %></span>
    </div>
    <div class="top-bar-middle">
      <span class="nav-links nav-title"><%= link_to  'Recipedia', "/" %></span>
    </div>
    <div class="top-bar-right">
      <% if signed_in? %>
        <span class="nav-text nav-links"><%= link_to "Profile", "/user" %></span>
        <span class="nav-text nav-links"><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></span>
      <% else %>
        <span class="nav-text nav-links"><%= link_to "Sign In", new_user_session_path %></span>
        <span class="nav-text nav-links"><%= link_to "Sign Up", new_user_registration_path %></span>
      <% end %>
    </div>
  </div>

  <%= yield %>

  <%= javascript_include_tag 'application' %>
  
  <%- if Rails.env.development? || Rails.env.test? -%> 
    <%= javascript_tag do %>
      window.railsEnv = '<%= Rails.env %>'
    <%- end -%>
  <%- end -%>
  <%= javascript_pack_tag 'application' %>
  
  <%= yield :extra_footer %>
</body>
</html>
